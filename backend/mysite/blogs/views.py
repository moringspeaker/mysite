from django.shortcuts import render
from .models import Blog
from .models import Category

# def return_blog(request):
#     blogs = Blog.objects.all()
#     return render(request, 'index.html', {'blog': blogs})
# Create your views here.
